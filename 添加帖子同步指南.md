# 添加帖子并同步到 JSON 文件指南

## 🎯 目标

当用户添加新帖子（PostCell）时，需要：
1. ✅ 保存到 SwiftData 数据库
2. ✅ 同步到本地 JSON 文件
3. ✅ 自动更新展示页面

## 📋 完整流程

### 步骤 1: 用户点击"添加"按钮

```swift
// 在 PostListView 中
@State private var showingAddPost = false

Button(action: {
    showingAddPost = true
}) {
    Image(systemName: "plus")
}
.sheet(isPresented: $showingAddPost) {
    AddPostView()
}
```

### 步骤 2: 用户在 AddPostView 中输入信息

```swift
struct AddPostView: View {
    @State private var name = ""
    @State private var text = ""
    @State private var avatar = ""
    @State private var images: [String] = []
    // ... 其他字段
}
```

### 步骤 3: 用户点击"保存"按钮

触发 `savePost()` 方法：

```swift
private func savePost() {
    // 1. 生成新的唯一 ID
    let descriptor = FetchDescriptor<Post>(
        sortBy: [SortDescriptor(\.id, order: .reverse)]
    )
    let existingPosts = try? modelContext.fetch(descriptor)
    let newId = (existingPosts?.first?.id ?? 0) + 1
    
    // 2. 创建新的 Post 对象
    let dateFormatter = DateFormatter()
    dateFormatter.dateFormat = "yyyy-MM-dd HH:mm"
    let dateString = dateFormatter.string(from: Date())
    
    let newPost = Post(
        id: newId,
        avatar: avatar.isEmpty ? "default_avatar.jpg" : avatar,
        vip: false,
        name: name,
        date: dateString,
        isFollowed: false,
        text: text,
        images: images,
        commentCount: 0,
        likeCount: 0,
        isLiked: false
    )
    
    // 3. 插入到 SwiftData
    modelContext.insert(newPost)
    try? modelContext.save()
    
    // 4. 同步到 JSON 文件
    JSONService.savePostsToJSON(
        fileName: "PostListData_recommend_1.json",
        modelContext: modelContext
    )
    
    // 5. 关闭添加视图
    dismiss()
}
```

### 步骤 4: 自动更新展示页面

由于使用了 `@Query`，当数据变化时，视图会自动更新：

```swift
@Query(sort: \Post.id) private var posts: [Post]
// ↑ 这个查询会自动监听 SwiftData 的变化
// 当 modelContext.insert() 和 save() 执行后
// posts 数组会自动更新
// PostListView 会自动刷新显示新帖子
```

## 🔄 同步机制详解

### 同步时机

#### 1. 添加新帖子时（实时同步）

```swift
// 在 savePost() 方法中
modelContext.insert(newPost)
try? modelContext.save()
JSONService.savePostsToJSON(...)  // 立即同步
```

**优点**:
- 数据立即持久化
- 不会丢失数据

**缺点**:
- 频繁写入可能影响性能（数据量大时）

#### 2. 修改帖子时（实时同步）

```swift
// 在 PostCellView 中
.onChange(of: post.isLiked) { _, _ in
    JSONService.savePostsToJSON(...)  // 立即同步
}
```

#### 3. 删除帖子时（实时同步）

```swift
// 在 PostListView 中
private func deletePosts(offsets: IndexSet) {
    for index in offsets {
        modelContext.delete(posts[index])
    }
    JSONService.savePostsToJSON(...)  // 立即同步
}
```

### JSONService.savePostsToJSON() 工作原理

```swift
static func savePostsToJSON(
    fileName: String,
    modelContext: ModelContext
) -> Bool {
    // 1. 从 SwiftData 获取所有 Post
    let descriptor = FetchDescriptor<Post>(
        sortBy: [SortDescriptor(\.id)]
    )
    let posts = try? modelContext.fetch(descriptor)
    
    // 2. 转换为 JSON 格式
    let jsonArray = posts?.map { $0.toJSON() } ?? []
    let json: [String: Any] = ["list": jsonArray]
    
    // 3. 保存到 Documents 目录（可写）
    guard let documentsURL = FileManager.default.urls(
        for: .documentDirectory,
        in: .userDomainMask
    ).first else {
        return false
    }
    
    let fileURL = documentsURL.appendingPathComponent(fileName)
    
    // 4. 写入 JSON 数据
    let jsonData = try JSONSerialization.data(
        withJSONObject: json,
        options: [.prettyPrinted, .sortedKeys]
    )
    try? jsonData.write(to: fileURL)
    
    return true
}
```

**重要说明**:
- Bundle 中的文件是**只读**的
- 保存到 **Documents 目录**（可写）
- 文件路径: `~/Documents/PostListData_recommend_1.json`

## 📊 数据流向图

```
用户输入
    ↓
AddPostView.savePost()
    ↓
创建 Post 对象
    ↓
modelContext.insert(newPost)  ← 插入到 SwiftData
    ↓
modelContext.save()           ← 保存到数据库
    ↓
@Query 自动检测变化           ← 触发视图更新
    ↓
PostListView 自动刷新         ← 显示新帖子
    ↓
JSONService.savePostsToJSON() ← 同步到 JSON 文件
    ↓
Documents/PostListData_recommend_1.json  ← 文件已更新
```

## 🎨 视图自动更新原理

### @Query 的工作原理

```swift
@Query(sort: \Post.id) private var posts: [Post]
```

**@Query 的特性**:
1. **自动查询**: 应用启动时自动从 SwiftData 查询数据
2. **自动监听**: 监听 SwiftData 数据变化
3. **自动更新**: 数据变化时自动重新查询
4. **自动刷新**: 视图自动刷新显示最新数据

**触发更新的操作**:
- ✅ `modelContext.insert()` - 插入新数据
- ✅ `modelContext.delete()` - 删除数据
- ✅ `modelContext.save()` - 保存更改
- ✅ 修改 `@Bindable` 属性并保存

### 为什么不需要手动刷新？

```swift
// ❌ 不需要这样做
@State private var refreshID = UUID()
Button("刷新") {
    refreshID = UUID()
}

// ✅ SwiftData + @Query 自动处理
@Query private var posts: [Post]
// 数据变化时自动更新，无需手动刷新
```

## 🔧 优化建议

### 方案 A: 实时同步（当前实现）

**适用场景**: 数据量小（< 1000 条）

```swift
// 每次操作后立即同步
modelContext.save()
JSONService.savePostsToJSON(...)
```

### 方案 B: 批量同步

**适用场景**: 数据量大（> 1000 条）

```swift
// 在视图消失时同步
.onDisappear {
    JSONService.savePostsToJSON(...)
}

// 或定时同步（每 30 秒）
Timer.publish(every: 30, on: .main, in: .common)
    .autoconnect()
    .sink { _ in
        JSONService.savePostsToJSON(...)
    }
```

### 方案 C: 手动同步

**适用场景**: 用户控制同步时机

```swift
Button("同步到 JSON") {
    JSONService.savePostsToJSON(...)
}
```

## 📝 完整代码示例

### AddPostView 完整实现

```swift
struct AddPostView: View {
    @Environment(\.modelContext) private var modelContext
    @Environment(\.dismiss) private var dismiss
    
    @State private var name = ""
    @State private var text = ""
    @State private var avatar = ""
    @State private var images: [String] = []
    @State private var newImageName = ""
    
    var body: some View {
        NavigationView {
            Form {
                Section("用户信息") {
                    TextField("用户名", text: $name)
                    TextField("头像文件名", text: $avatar)
                }
                
                Section("内容") {
                    TextEditor(text: $text)
                        .frame(height: 100)
                }
                
                Section("图片") {
                    ForEach(images, id: \.self) { imageName in
                        Text(imageName)
                    }
                    .onDelete(perform: deleteImage)
                    
                    HStack {
                        TextField("图片文件名", text: $newImageName)
                        Button("添加") {
                            if !newImageName.isEmpty {
                                images.append(newImageName)
                                newImageName = ""
                            }
                        }
                    }
                }
            }
            .navigationTitle("添加帖子")
            .toolbar {
                ToolbarItem(placement: .navigationBarLeading) {
                    Button("取消") {
                        dismiss()
                    }
                }
                ToolbarItem(placement: .navigationBarTrailing) {
                    Button("保存") {
                        savePost()
                    }
                    .disabled(name.isEmpty || text.isEmpty)
                }
            }
        }
    }
    
    private func deleteImage(offsets: IndexSet) {
        images.remove(atOffsets: offsets)
    }
    
    private func savePost() {
        // 生成新 ID
        let descriptor = FetchDescriptor<Post>(
            sortBy: [SortDescriptor(\.id, order: .reverse)]
        )
        let existingPosts = try? modelContext.fetch(descriptor)
        let newId = (existingPosts?.first?.id ?? 0) + 1
        
        // 格式化日期
        let dateFormatter = DateFormatter()
        dateFormatter.dateFormat = "yyyy-MM-dd HH:mm"
        let dateString = dateFormatter.string(from: Date())
        
        // 创建新 Post
        let newPost = Post(
            id: newId,
            avatar: avatar.isEmpty ? "default_avatar.jpg" : avatar,
            vip: false,
            name: name,
            date: dateString,
            isFollowed: false,
            text: text,
            images: images,
            commentCount: 0,
            likeCount: 0,
            isLiked: false
        )
        
        // 保存到 SwiftData
        modelContext.insert(newPost)
        try? modelContext.save()
        
        // 同步到 JSON
        JSONService.savePostsToJSON(
            fileName: "PostListData_recommend_1.json",
            modelContext: modelContext
        )
        
        // 关闭视图（视图会自动更新，因为 @Query 检测到变化）
        dismiss()
    }
}
```

## ✅ 验证步骤

1. **添加帖子**
   - 点击"+"按钮
   - 输入信息
   - 点击"保存"
   - ✅ 帖子应该立即出现在列表中

2. **检查 SwiftData**
   - 应用重新启动
   - ✅ 帖子应该还在（持久化成功）

3. **检查 JSON 文件**
   - 查看 `Documents/PostListData_recommend_1.json`
   - ✅ 应该包含新添加的帖子

4. **修改帖子**
   - 点击点赞按钮
   - ✅ 点赞数应该更新
   - ✅ JSON 文件应该同步更新

5. **删除帖子**
   - 滑动删除
   - ✅ 帖子应该从列表消失
   - ✅ JSON 文件应该同步更新

## 🐛 常见问题

### Q1: 添加后看不到新帖子？

**A**: 检查是否调用了 `modelContext.save()`

```swift
modelContext.insert(newPost)
try? modelContext.save()  // ← 必须调用
```

### Q2: JSON 文件没有更新？

**A**: 检查文件路径和权限

```swift
// 确保保存到 Documents 目录（可写）
let documentsURL = FileManager.default.urls(
    for: .documentDirectory,
    in: .userDomainMask
).first
```

### Q3: 视图不自动更新？

**A**: 确保使用 `@Query` 而不是手动查询

```swift
// ✅ 正确
@Query(sort: \Post.id) private var posts: [Post]

// ❌ 错误
@State private var posts: [Post] = []
```

### Q4: 如何查看保存的 JSON 文件？

**A**: 使用 Xcode 的 Device 窗口

1. Window → Devices and Simulators
2. 选择设备/模拟器
3. 选择应用
4. 下载容器
5. 查看 `Documents/PostListData_recommend_1.json`

## 📚 总结

通过以上步骤，你可以：

1. ✅ 添加新帖子到 SwiftData
2. ✅ 自动同步到 JSON 文件
3. ✅ 视图自动更新显示新帖子
4. ✅ 数据持久化保存

整个过程是**自动化**的，无需手动刷新视图！🎉
